package states.screen;

import states.common.IState;

/**
 * state handling a choice picker ui element.
 * when created, must be given a list of options and callbacks.
 */
class ChoiceState implements IState{
    
}