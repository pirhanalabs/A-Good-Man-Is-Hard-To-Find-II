package states.screen;

import states.common.IState;

/**
 * state that displays the player's inventory and
 * allow actions on it, like deleting spoiled ingredients.
 */
class InventoryState implements IState{
    
}